API_URL = "/wiz/admin/api/file";
LOCALSTORAGEID = "season.wiz.resources.properties";
TREE_DATA = [
    { path: BRANCHPATH + "/interfaces", name: "model", type: 'folder', icon: 'fa-layer-group', display: "Model" }
];

VIEWER_IMAGE_URL = (item) => { 
    return false;
};